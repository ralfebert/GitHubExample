name: GitHubExample
options:
  bundleIdPrefix: com.example
  generateEmptyDirectories: true
  deploymentTarget:
    iOS: "13.0"
targets:
  GitHubExample:
    type: application
    platform: iOS
    sources:
      - path: GitHubExample
        excludes:
          - "Assets/*.sketch"
    settings:
      base:
        SWIFT_VERSION: 5.1